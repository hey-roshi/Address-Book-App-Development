# Dev-specific configurations (if any)
# Example: Use a different database for development
# spring.datasource.url=jdbc:mysql://localhost:3306/addressbook_dev_db

# Dev-specific logging configuration (optional)
logging.level.com.development=DEBUG
logging.file.name=dev-app.log